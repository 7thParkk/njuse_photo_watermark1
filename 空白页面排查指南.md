# 空白页面问题排查步骤

## 问题描述
打开 http://localhost:3000/ 页面显示空白

## 可能的原因

1. **JavaScript 错误** - 检查浏览器控制台（F12）
2. **Supabase 配置未设置** - 导致初始化失败
3. **React 渲染错误** - 组件抛出异常
4. **CSS 加载问题** - Tailwind CSS 未正确编译

## 排查步骤

### 1. 检查浏览器控制台
1. 按 F12 打开开发者工具
2. 查看 Console 标签页是否有错误信息
3. 查看是否有红色错误提示

### 2. 检查网络请求
1. 打开 Network 标签页
2. 刷新页面
3. 检查是否有失败的请求（红色）
4. 检查 main.tsx 是否成功加载

### 3. 检查页面元素
1. 右键页面 -> 检查（或按 F12）
2. 查看 Elements 标签页
3. 检查是否有 `<div id="root"></div>` 元素
4. 检查 root 元素是否有子元素

### 4. 手动测试
在浏览器控制台运行以下代码：

```javascript
// 检查根元素
console.log('Root元素:', document.getElementById('root'));

// 检查是否有React错误
console.log('React DevTools:', window.__REACT_DEVTOOLS_GLOBAL_HOOK__);

// 检查LocalStorage
console.log('API配置:', localStorage.getItem('api_config'));

// 检查是否有错误
console.log('页面错误:', window.onerror);
```

## 已修复的问题

1. ✅ `postcss.config.js` - 修复了 ES module 语法问题
2. ✅ `src/services/supabase.ts` - 添加了更好的错误处理
3. ✅ `src/stores/authStore.ts` - 添加了错误处理，确保即使配置未设置也能显示页面

## 解决方案

### 方案1：确保 Supabase 配置
1. 访问 http://localhost:3000/settings（如果可以直接访问）
2. 填写 Supabase 配置：
   - Project URL: `https://chafoftrodhhuxhkmzpu.supabase.co`
   - Anon Key: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`（完整key见prepare.md）

### 方案2：临时绕过（用于测试）
如果只是想测试页面显示，可以：
1. 打开浏览器控制台
2. 运行：`localStorage.setItem('api_config', JSON.stringify({supabase: {url: 'https://chafoftrodhhuxhkmzpu.supabase.co', anonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNoYWZvZnRyb2RoaHV4aGttenB1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIxNTUyMzQsImV4cCI6MjA3NzczMTIzNH0.u1UOhna5_nmU-EPL2ixAq7s3vEHUsoA-vByGjavdQOc'}}))`
3. 刷新页面

### 方案3：检查构建错误
```bash
# 尝试构建项目看是否有错误
npm run build
```

## 如果问题仍然存在

请提供以下信息：
1. 浏览器控制台的完整错误信息（截图或复制文本）
2. Network 标签页中失败的请求
3. Console 标签页中的所有错误和警告

