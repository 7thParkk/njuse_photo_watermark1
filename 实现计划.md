# AI 旅行规划师 - 实现计划

## 一、项目架构设计

### 1.1 技术栈选择
- **前端框架**: React + TypeScript
- **构建工具**: Vite
- **样式方案**: Tailwind CSS
- **状态管理**: Zustand
- **路由管理**: React Router
- **HTTP客户端**: Axios
- **地图服务**: 高德地图 JavaScript API
- **语音识别**: 科大讯飞 WebAPI
- **后端服务**: Supabase (认证 + 数据库)
- **AI服务**: 大语言模型 API (用户配置)

### 1.2 项目结构
```
njuse_photo_watermark1/
├── src/
│   ├── components/          # 可复用组件
│   │   ├── Map/            # 地图组件
│   │   ├── VoiceInput/     # 语音输入组件
│   │   ├── TravelPlan/     # 行程展示组件
│   │   ├── BudgetManager/  # 预算管理组件
│   │   └── Settings/       # 设置页面组件
│   ├── pages/              # 页面组件
│   │   ├── Home/           # 首页
│   │   ├── Login/          # 登录页
│   │   ├── Register/       # 注册页
│   │   ├── Planner/        # 行程规划页
│   │   └── Settings/       # 设置页
│   ├── services/           # API服务层
│   │   ├── supabase.ts     # Supabase客户端
│   │   ├── voice.ts        # 语音识别服务
│   │   ├── map.ts          # 地图服务
│   │   ├── ai.ts           # AI服务
│   │   └── api.ts          # 通用API工具
│   ├── stores/             # 状态管理
│   │   ├── authStore.ts    # 认证状态
│   │   ├── planStore.ts    # 行程状态
│   │   └── configStore.ts  # 配置状态
│   ├── utils/              # 工具函数
│   │   ├── config.ts       # 配置管理
│   │   └── storage.ts      # 本地存储
│   ├── types/              # TypeScript类型定义
│   ├── App.tsx             # 根组件
│   └── main.tsx            # 入口文件
├── public/                 # 静态资源
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── README.md
```

## 二、功能模块实现计划

### 2.1 用户认证模块
**优先级**: 高
**实现步骤**:
1. 集成 Supabase 客户端
2. 创建登录/注册页面
3. 实现用户状态管理
4. 添加路由守卫

### 2.2 API配置管理模块
**优先级**: 高
**实现步骤**:
1. 创建配置存储机制（localStorage + Supabase）
2. 创建设置页面，支持用户输入API密钥
3. 实现配置验证功能
4. 确保API密钥不提交到代码库

### 2.3 语音识别模块
**优先级**: 高
**实现步骤**:
1. 集成科大讯飞 WebAPI
2. 创建语音输入组件
3. 实现录音功能
4. 实现语音转文字功能
5. 添加语音输入UI反馈

### 2.4 地图服务模块
**优先级**: 高
**实现步骤**:
1. 集成高德地图 JavaScript API
2. 创建地图组件
3. 实现地点搜索功能
4. 实现路线规划展示
5. 实现标记点管理

### 2.5 AI行程规划模块
**优先级**: 高
**实现步骤**:
1. 集成大语言模型API
2. 设计行程规划提示词模板
3. 实现用户需求解析
4. 实现行程生成功能
5. 实现行程结构化处理

### 2.6 费用预算管理模块
**优先级**: 中
**实现步骤**:
1. 创建预算记录数据结构
2. 实现预算录入功能（支持语音）
3. 实现预算统计分析
4. 实现预算可视化展示

### 2.7 数据存储与同步模块
**优先级**: 中
**实现步骤**:
1. 设计 Supabase 数据库表结构
2. 实现行程数据存储
3. 实现数据同步功能
4. 实现离线缓存机制

### 2.8 UI/UX实现
**优先级**: 中
**实现步骤**:
1. 设计响应式布局
2. 实现地图为主的主界面
3. 实现行程卡片展示
4. 添加美观的动画效果
5. 优化移动端体验

## 三、数据库设计

### 3.1 users 表（Supabase自动管理）

### 3.2 travel_plans 表
```sql
CREATE TABLE travel_plans (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  destination TEXT NOT NULL,
  start_date DATE,
  end_date DATE,
  budget DECIMAL,
  travelers INTEGER,
  preferences JSONB,
  itinerary JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 3.3 budget_records 表
```sql
CREATE TABLE budget_records (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  plan_id UUID REFERENCES travel_plans(id) ON DELETE CASCADE,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  category TEXT NOT NULL,
  amount DECIMAL NOT NULL,
  description TEXT,
  date DATE DEFAULT CURRENT_DATE,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 3.4 api_configs 表（可选，也可用localStorage）
```sql
CREATE TABLE api_configs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  ai_api_url TEXT,
  ai_api_key TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

## 四、开发顺序

1. **阶段1**: 项目初始化和基础配置
   - 创建项目结构
   - 配置开发环境
   - 设置API配置管理

2. **阶段2**: 用户认证系统
   - 集成Supabase
   - 实现登录/注册
   - 实现路由守卫

3. **阶段3**: 核心功能开发
   - 语音识别功能
   - 地图服务集成
   - AI服务集成

4. **阶段4**: 行程规划功能
   - 实现需求解析
   - 实现行程生成
   - 实现行程展示

5. **阶段5**: 预算管理和数据同步
   - 实现预算记录
   - 实现数据存储
   - 实现云端同步

6. **阶段6**: UI优化和测试
   - 优化界面设计
   - 添加动画效果
   - 测试和修复bug

## 五、注意事项

1. **API密钥安全**: 所有API密钥必须通过配置页面输入，不硬编码在代码中
2. **环境变量**: 使用 `.env.local` 文件存储开发环境的API密钥（已添加到.gitignore）
3. **错误处理**: 所有API调用都需要完善的错误处理
4. **用户体验**: 添加加载状态、错误提示等用户反馈
5. **响应式设计**: 确保在移动端和桌面端都有良好的体验

