# 功能测试计划

## 测试环境准备

### 1. 安装依赖
```bash
npm install
```

### 2. 初始化数据库
在 Supabase Dashboard 中执行 `supabase_init.sql`

### 3. 启动开发服务器
```bash
npm run dev
```

---

## 测试用例

### 测试1：项目基础配置 ✅

**测试步骤**：
1. 检查 package.json 是否正确
2. 运行 `npm install` 安装依赖
3. 检查 TypeScript 配置
4. 检查 Tailwind CSS 配置

**预期结果**：
- ✅ 依赖安装成功
- ✅ 无编译错误
- ✅ 项目可以正常启动

**实际结果**：
[待测试]

---

### 测试2：用户认证功能

#### 2.1 用户注册

**测试步骤**：
1. 访问 http://localhost:3000
2. 点击"立即注册"
3. 输入邮箱和密码（至少6位）
4. 点击注册

**预期结果**：
- ✅ 注册成功提示
- ✅ 自动跳转到登录页面
- ✅ 数据库中有新用户记录

**实际结果**：
[待测试]

#### 2.2 用户登录

**测试步骤**：
1. 在登录页面输入注册的邮箱和密码
2. 点击登录

**预期结果**：
- ✅ 登录成功
- ✅ 自动跳转到首页
- ✅ 显示用户信息

**实际结果**：
[待测试]

#### 2.3 路由守卫

**测试步骤**：
1. 未登录状态下访问 /planner
2. 未登录状态下访问 /settings

**预期结果**：
- ✅ 自动跳转到登录页面

**实际结果**：
[待测试]

#### 2.4 用户登出

**测试步骤**：
1. 登录状态下点击"退出登录"
2. 确认退出

**预期结果**：
- ✅ 退出成功
- ✅ 跳转到登录页面
- ✅ 无法访问受保护页面

**实际结果**：
[待测试]

---

### 测试3：API配置管理

#### 3.1 配置页面显示

**测试步骤**：
1. 登录后访问设置页面
2. 检查所有API配置输入框是否显示

**预期结果**：
- ✅ 显示所有API配置项
- ✅ 默认值已填充（如果有）

**实际结果**：
[待测试]

#### 3.2 保存配置

**测试步骤**：
1. 填写所有API配置
2. 点击"保存配置"

**预期结果**：
- ✅ 配置保存成功提示
- ✅ 配置存储在 localStorage
- ✅ 页面刷新后配置仍然存在

**实际结果**：
[待测试]

#### 3.3 配置验证

**测试步骤**：
1. 不填写某些必填项
2. 点击保存

**预期结果**：
- ✅ 显示缺失配置提示
- ✅ 配置不完整时不保存

**实际结果**：
[待测试]

---

### 测试4：语音识别功能

#### 4.1 语音输入组件显示

**测试步骤**：
1. 访问行程规划页面
2. 检查语音输入按钮是否显示

**预期结果**：
- ✅ 显示"开始录音"按钮

**实际结果**：
[待测试]

#### 4.2 录音功能

**测试步骤**：
1. 点击"开始录音"
2. 允许浏览器麦克风权限
3. 说话几秒钟
4. 点击"停止录音"

**预期结果**：
- ✅ 成功获取麦克风权限
- ✅ 录音状态显示正常
- ✅ 停止录音后显示"正在处理..."

**实际结果**：
[待测试]

#### 4.3 语音识别结果

**测试步骤**：
1. 完成录音后等待识别结果

**预期结果**：
- ⚠️ 由于浏览器限制，可能提示使用文字输入
- ✅ 如果有错误，错误信息友好

**实际结果**：
[待测试]

---

### 测试5：地图服务功能

#### 5.1 地图加载

**测试步骤**：
1. 配置高德地图API
2. 访问行程规划页面
3. 检查右侧地图是否加载

**预期结果**：
- ✅ 地图正常显示
- ✅ 默认显示北京地区

**实际结果**：
[待测试]

#### 5.2 地点搜索（API测试）

**测试步骤**：
1. 在代码中调用 MapService.searchPlace("北京天安门")
2. 检查返回结果

**预期结果**：
- ✅ 返回地点列表
- ✅ 包含坐标信息

**实际结果**：
[待测试]

#### 5.3 地理编码（API测试）

**测试步骤**：
1. 调用 MapService.geocode("北京市天安门")
2. 检查返回结果

**预期结果**：
- ✅ 返回经纬度坐标

**实际结果**：
[待测试]

---

### 测试6：AI行程规划功能

#### 6.1 文字输入

**测试步骤**：
1. 在行程规划页面输入文字："我想去日本，5天，预算1万元，喜欢美食"
2. 点击"开始规划"

**预期结果**：
- ✅ 显示"规划中..."状态
- ✅ 成功调用AI API
- ✅ 返回行程数据

**实际结果**：
[待测试]

#### 6.2 行程数据解析

**测试步骤**：
1. 检查AI返回的数据格式
2. 验证数据是否正确解析

**预期结果**：
- ✅ 数据格式正确
- ✅ 包含 title, destination, itinerary 等字段

**实际结果**：
[待测试]

#### 6.3 行程保存

**测试步骤**：
1. 规划完成后检查是否保存到数据库
2. 刷新页面检查数据是否还在

**预期结果**：
- ✅ 行程保存成功
- ✅ 数据库中有记录
- ✅ 刷新后数据仍存在

**实际结果**：
[待测试]

#### 6.4 行程显示

**测试步骤**：
1. 规划完成后检查左侧是否显示行程详情
2. 检查每一天的活动是否显示

**预期结果**：
- ✅ 显示行程标题和目的地
- ✅ 显示每天的安排
- ✅ 显示活动时间和费用

**实际结果**：
[待测试]

---

### 测试7：预算管理功能

#### 7.1 预算记录添加

**测试步骤**：
1. 在已有行程页面点击"添加记录"
2. 填写类别、金额、描述
3. 点击保存

**预期结果**：
- ✅ 记录添加成功
- ✅ 立即显示在列表中
- ✅ 数据库中有记录

**实际结果**：
[待测试]

#### 7.2 预算统计

**测试步骤**：
1. 添加多条预算记录
2. 检查总支出是否正确计算

**预期结果**：
- ✅ 总支出金额正确
- ✅ 实时更新

**实际结果**：
[待测试]

#### 7.3 预算记录删除

**测试步骤**：
1. 点击某条记录的删除按钮
2. 确认删除

**预期结果**：
- ✅ 记录删除成功
- ✅ 从列表中移除
- ✅ 总支出重新计算

**实际结果**：
[待测试]

---

### 测试8：数据存储和同步

#### 8.1 行程列表显示

**测试步骤**：
1. 访问首页
2. 检查是否显示所有行程

**预期结果**：
- ✅ 显示所有已保存的行程
- ✅ 显示行程标题、目的地、日期等信息

**实际结果**：
[待测试]

#### 8.2 行程编辑

**测试步骤**：
1. 点击某个行程卡片
2. 修改行程需求
3. 重新规划

**预期结果**：
- ✅ 加载原有行程数据
- ✅ 可以更新行程
- ✅ 更新后数据保存

**实际结果**：
[待测试]

#### 8.3 行程删除

**测试步骤**：
1. 在首页点击行程的删除按钮
2. 确认删除

**预期结果**：
- ✅ 行程删除成功
- ✅ 从列表中移除
- ✅ 数据库记录删除

**实际结果**：
[待测试]

---

## 测试总结

### 通过的功能
- [ ] 用户认证
- [ ] API配置管理
- [ ] 语音识别（基础功能）
- [ ] 地图服务
- [ ] AI行程规划
- [ ] 预算管理
- [ ] 数据存储

### 发现的问题
1. [待记录]

### 需要修复的问题
1. [待记录]

