# 功能测试脚本

## 测试前准备

### 1. 确保依赖已安装
```bash
npm install
```

### 2. 检查项目结构
确认以下文件存在：
- ✅ package.json
- ✅ vite.config.ts
- ✅ tsconfig.json
- ✅ tailwind.config.js
- ✅ postcss.config.js
- ✅ src/main.tsx
- ✅ src/App.tsx

### 3. 检查代码编译
```bash
npm run build
```

如果没有错误，说明代码结构正确。

---

## 逐步功能测试

### 测试1：项目基础配置 ✅

**检查项**：
- [ ] package.json 依赖是否正确
- [ ] TypeScript 配置是否正确
- [ ] Tailwind CSS 配置是否正确
- [ ] 项目能否正常启动

**测试命令**：
```bash
npm run dev
```

**预期结果**：
- ✅ 启动成功，显示 "VITE v5.x.x ready in xxx ms"
- ✅ 浏览器自动打开 http://localhost:3000
- ✅ 页面显示登录界面

**实际结果**：
[在此记录测试结果]

---

### 测试2：用户认证功能

#### 2.1 用户注册

**测试步骤**：
1. 访问 http://localhost:3000
2. 点击"立即注册"
3. 输入邮箱：`test@example.com`
4. 输入密码：`test123456`（至少6位）
5. 输入确认密码：`test123456`
6. 点击"注册"

**预期结果**：
- ✅ 显示"注册成功！请检查邮箱验证链接（如果有）"
- ✅ 自动跳转到登录页面
- ✅ Supabase 中有新用户记录（可通过 Supabase Dashboard 查看）

**实际结果**：
[在此记录测试结果]

**问题记录**：
[如有问题，在此记录]

#### 2.2 用户登录

**测试步骤**：
1. 在登录页面输入邮箱：`test@example.com`
2. 输入密码：`test123456`
3. 点击"登录"

**预期结果**：
- ✅ 登录成功
- ✅ 自动跳转到首页（/）
- ✅ 页面显示"我的行程"和"创建新行程"按钮

**实际结果**：
[在此记录测试结果]

#### 2.3 路由守卫测试

**测试步骤**：
1. 在登录状态下，复制以下URL：`http://localhost:3000/planner`
2. 打开新标签页访问
3. 关闭浏览器标签页
4. 清除 localStorage（F12 -> Application -> Local Storage -> Clear）
5. 刷新页面

**预期结果**：
- ✅ 登录状态下可以访问受保护页面
- ✅ 清除登录状态后，访问受保护页面会自动跳转到登录页

**实际结果**：
[在此记录测试结果]

#### 2.4 用户登出

**测试步骤**：
1. 登录状态下点击"退出"按钮
2. 确认退出

**预期结果**：
- ✅ 退出成功
- ✅ 跳转到登录页面
- ✅ 无法访问受保护页面

**实际结果**：
[在此记录测试结果]

---

### 测试3：API配置管理

#### 3.1 访问设置页面

**测试步骤**：
1. 登录后点击导航栏的"设置"
2. 或直接访问：`http://localhost:3000/settings`

**预期结果**：
- ✅ 显示设置页面
- ✅ 显示所有API配置输入框：
  - 科大讯飞语音识别API（APPID, APISecret, APIKey）
  - 高德地图API（Web服务Key, JavaScript API Key, JavaScript API Secret）
  - 大语言模型API（Base URL, API Key）
  - Supabase配置（Project URL, Anon Key）

**实际结果**：
[在此记录测试结果]

#### 3.2 保存配置

**测试步骤**：
1. 填写所有API配置（使用 prepare.md 中的测试密钥）
2. 点击"保存配置"

**预期结果**：
- ✅ 显示"配置保存成功！"
- ✅ 页面自动刷新
- ✅ 打开浏览器控制台（F12），检查 localStorage：
  ```javascript
  JSON.parse(localStorage.getItem('api_config'))
  ```
  - ✅ 应该能看到保存的配置

**实际结果**：
[在此记录测试结果]

#### 3.3 配置验证

**测试步骤**：
1. 清空某些必填项（如高德地图的 Web服务Key）
2. 点击"保存配置"

**预期结果**：
- ✅ 显示"配置不完整：高德地图API"等提示
- ✅ 配置不保存

**实际结果**：
[在此记录测试结果]

---

### 测试4：语音识别功能

#### 4.1 语音输入组件显示

**测试步骤**：
1. 访问行程规划页面：`http://localhost:3000/planner`
2. 检查左侧输入区域

**预期结果**：
- ✅ 显示"开始录音"按钮
- ✅ 显示文本输入框

**实际结果**：
[在此记录测试结果]

#### 4.2 录音功能

**测试步骤**：
1. 点击"开始录音"按钮
2. 允许浏览器麦克风权限
3. 说话几秒钟（例如："我想去日本旅行"）
4. 点击"停止录音"

**预期结果**：
- ✅ 成功获取麦克风权限
- ✅ 按钮变为"停止录音"（红色）
- ✅ 显示"正在录音..."状态
- ✅ 停止后显示"正在处理..."或"正在识别..."

**实际结果**：
[在此记录测试结果]

#### 4.3 语音识别结果

**测试步骤**：
1. 等待识别完成

**预期结果**：
- ⚠️ 由于浏览器限制，可能显示"语音识别功能需要后端服务支持，请使用文字输入"
- ✅ 如果失败，错误信息友好，提示用户使用文字输入

**实际结果**：
[在此记录测试结果]

**注意**：语音识别功能需要后端服务支持，当前版本主要支持文字输入。

---

### 测试5：地图服务功能

#### 5.1 地图加载

**测试步骤**：
1. 确保已配置高德地图API
2. 访问行程规划页面
3. 查看右侧地图区域

**预期结果**：
- ✅ 地图正常显示
- ✅ 默认显示北京地区（坐标 [116.397428, 39.90923]）
- ✅ 可以拖动和缩放地图

**实际结果**：
[在此记录测试结果]

#### 5.2 地图API测试（控制台测试）

**测试步骤**：
1. 打开浏览器控制台（F12）
2. 在 Console 中输入：
```javascript
import { MapService } from './src/services/api.js';
const mapService = new MapService();
mapService.searchPlace('北京天安门').then(console.log);
```

**预期结果**：
- ✅ 返回地点列表
- ✅ 包含地点名称、坐标等信息

**实际结果**：
[在此记录测试结果]

---

### 测试6：AI行程规划功能

#### 6.1 文字输入和规划

**测试步骤**：
1. 确保已配置大语言模型API
2. 在行程规划页面的文本框中输入：
   ```
   我想去日本，5天，预算1万元，喜欢美食和动漫，带孩子
   ```
3. 点击"开始规划"按钮

**预期结果**：
- ✅ 按钮变为"规划中..."（禁用状态）
- ✅ 显示加载状态
- ✅ 成功调用AI API
- ✅ 返回行程数据

**实际结果**：
[在此记录测试结果]

#### 6.2 行程数据解析

**测试步骤**：
1. 等待规划完成
2. 检查左侧显示的行程信息

**预期结果**：
- ✅ 显示行程标题（如"日本5日游"）
- ✅ 显示目的地：日本
- ✅ 显示预算：¥10000
- ✅ 显示行程安排列表

**实际结果**：
[在此记录测试结果]

#### 6.3 行程保存

**测试步骤**：
1. 规划完成后，检查：
   - 是否显示"行程规划完成！"提示
   - URL是否变为 `/planner?id=xxx`
2. 刷新页面
3. 访问首页，检查是否显示新行程

**预期结果**：
- ✅ 显示成功提示
- ✅ URL包含行程ID
- ✅ 刷新后数据仍然存在
- ✅ 首页显示新创建的行程卡片

**实际结果**：
[在此记录测试结果]

#### 6.4 行程详情显示

**测试步骤**：
1. 在行程规划页面，检查左侧是否显示：
   - 行程标题和目的地
   - 每天的行程安排
   - 活动时间和费用

**预期结果**：
- ✅ 显示完整的行程信息
- ✅ 每一天的活动都正确显示
- ✅ 格式清晰易读

**实际结果**：
[在此记录测试结果]

---

### 测试7：预算管理功能

#### 7.1 预算记录添加

**测试步骤**：
1. 在已有行程页面（必须有已规划的行程）
2. 滚动到"费用记录"部分
3. 点击"添加记录"按钮
4. 填写：
   - 类别：选择"交通"
   - 金额：`500`
   - 描述：`机票费用`
5. 点击"保存"

**预期结果**：
- ✅ 记录添加成功
- ✅ 立即显示在列表中
- ✅ 总支出更新

**实际结果**：
[在此记录测试结果]

#### 7.2 预算统计

**测试步骤**：
1. 添加多条记录：
   - 交通：500
   - 住宿：2000
   - 餐饮：1500
2. 检查总支出显示

**预期结果**：
- ✅ 总支出显示：¥4000.00
- ✅ 计算正确

**实际结果**：
[在此记录测试结果]

#### 7.3 预算记录删除

**测试步骤**：
1. 点击某条记录的"删除"按钮
2. 确认删除

**预期结果**：
- ✅ 记录删除成功
- ✅ 从列表中移除
- ✅ 总支出重新计算

**实际结果**：
[在此记录测试结果]

---

### 测试8：数据存储和同步

#### 8.1 行程列表显示

**测试步骤**：
1. 访问首页：`http://localhost:3000/`
2. 检查是否显示所有已创建的行程

**预期结果**：
- ✅ 显示所有行程卡片
- ✅ 每个卡片显示：标题、目的地、日期、预算
- ✅ 可以点击卡片查看详情

**实际结果**：
[在此记录测试结果]

#### 8.2 行程编辑

**测试步骤**：
1. 点击某个行程卡片
2. 修改输入框中的需求
3. 点击"更新行程"

**预期结果**：
- ✅ 加载原有行程数据
- ✅ 可以修改并重新规划
- ✅ 更新后数据保存

**实际结果**：
[在此记录测试结果]

#### 8.3 行程删除

**测试步骤**：
1. 在首页点击某个行程的"删除"按钮
2. 确认删除

**预期结果**：
- ✅ 显示确认对话框
- ✅ 删除成功后从列表中移除
- ✅ Supabase数据库中记录也被删除

**实际结果**：
[在此记录测试结果]

#### 8.4 数据同步（多设备测试）

**测试步骤**：
1. 在一个浏览器中创建行程
2. 在另一个浏览器（或隐私模式）中登录同一账号
3. 检查是否能看到创建的行程

**预期结果**：
- ✅ 新浏览器中能看到所有行程
- ✅ 数据实时同步

**实际结果**：
[在此记录测试结果]

---

## 测试总结

### 通过的测试 ✅
- [ ] 测试1：项目基础配置
- [ ] 测试2：用户认证功能
- [ ] 测试3：API配置管理
- [ ] 测试4：语音识别功能
- [ ] 测试5：地图服务功能
- [ ] 测试6：AI行程规划功能
- [ ] 测试7：预算管理功能
- [ ] 测试8：数据存储和同步

### 发现的问题 🐛

1. **问题描述**：
   - **复现步骤**：
   - **预期行为**：
   - **实际行为**：
   - **严重程度**：[高/中/低]

2. [继续添加问题...]

### 需要修复的问题 🔧

1. [列出需要修复的问题]

### 测试环境信息
- 操作系统：
- 浏览器：
- Node.js 版本：
- npm 版本：
- 测试日期：

---

## 快速测试命令

### 代码检查
```bash
# TypeScript 类型检查
npm run build

# 如果使用 ESLint
npm run lint
```

### 启动开发服务器
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 预览生产版本
```bash
npm run preview
```
