# 开发和使用说明

## 项目初始化

### 1. 安装依赖

```bash
npm install
```

### 2. 初始化 Supabase 数据库

1. 登录 [Supabase Dashboard](https://app.supabase.com)
2. 选择你的项目（Project id: chafoftrodhhuxhkmzpu）
3. 进入 SQL Editor
4. 执行 `supabase_init.sql` 文件中的 SQL 语句来创建表结构和策略

### 3. 配置 API 密钥

启动应用后：

1. 访问登录页面，注册一个新账号
2. 登录后，进入"设置"页面
3. 配置所有必需的 API 密钥：
   - 科大讯飞语音识别 API
   - 高德地图 API
   - 大语言模型 API
   - Supabase 配置（默认已填写）

### 4. 启动开发服务器

```bash
npm run dev
```

应用将在 http://localhost:3000 启动

## 功能说明

### 用户认证
- 用户注册和登录功能
- 使用 Supabase Auth 进行身份验证
- 支持邮箱密码登录

### API 配置管理
- 安全的 API 密钥存储（localStorage）
- 支持用户自定义配置
- 配置验证功能

### 语音识别
- 基础语音输入组件
- ⚠️ 注意：完整的科大讯飞 WebSocket 语音识别需要后端服务支持
- 当前版本建议使用文字输入

### 地图服务
- 集成高德地图 JavaScript API
- 支持地点搜索和地理编码
- 地图展示功能

### AI 行程规划
- 集成大语言模型 API
- 根据用户需求生成详细行程
- 支持行程更新和保存

### 数据存储
- 使用 Supabase 存储行程数据
- 支持多设备同步
- Row Level Security 确保数据安全

## 注意事项

1. **API 密钥安全**：所有 API 密钥存储在 localStorage 中，不会提交到代码库
2. **语音识别**：完整的科大讯飞语音识别功能需要后端服务支持，前端直接调用 WebSocket 可能遇到跨域问题
3. **数据库初始化**：首次使用前必须执行 `supabase_init.sql` 初始化数据库
4. **CORS 问题**：如果遇到跨域问题，可能需要配置代理或使用后端服务

## 后续优化建议

1. 实现完整的语音识别后端服务
2. 增强地图交互功能（标记、路线规划等）
3. 实现预算管理功能
4. 添加行程分享功能
5. 优化移动端体验
6. 添加离线缓存功能

